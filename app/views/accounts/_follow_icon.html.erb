<% if is_follow.present? %>
  <%= link_to "Unfollow", follow_path(is_follow), method: :delete, remote: true,
    class: "btn btn-secondary" %>
<% else %>
  <%= link_to "Follow", account_follows_path(account), method: :post, remote: true,
    class: "btn btn-primary" %>
<% end %>