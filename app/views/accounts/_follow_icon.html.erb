<% if is_follow.present? %>
  <%= link_to "Love", follow_path(is_follow), method: :delete, remote: true,
    class: "core-sprite loved hide-text" %>
<% else %>
  <%= link_to "Loved", account_follows_path(account), method: :post, remote: true,
    class: "core-sprite love hide-text" %>
<% end %>